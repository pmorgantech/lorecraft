[package]
name = "lorecraft-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"

[dependencies]
lorecraft-protocol = { path = "../lorecraft-protocol" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
rand = "0.8"
rand_chacha = "0.3"
sha2 = "0.10"

[dev-dependencies]
